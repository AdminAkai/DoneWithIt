import { 
    StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
    login: {
        backgroundColor: '#fc5c65',
        width: '100%',
        height: 50,
        borderRadius: 50,
    },
    register: {
        backgroundColor: '#4ECDC4',
        width: '100%',
        height: 50,
        borderRadius: 50
    }
})

export default styles